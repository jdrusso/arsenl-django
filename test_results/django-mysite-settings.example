#Rename this to .django-mysite-settings-example and put it in ~/ for production
#Populate these fields with your info as needed

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql_psycopg2',
        'NAME': 'database',
        'USER': 'username',
        'PASSWORD': 'password'
    }
}

SECRET_KEY = 'put your django settings.py secret key here!'

JENKINS_USERNAME = 'username'
JENKINS_TOKEN = 'my-api-token

AUTH_LDAP_SERVER_URI = "ldap://your.ldap.server"
AUTH_LDAP_BIND_DN = "ldapuser"
AUTH_LDAP_BIND_PASSWORD = "ldappassword"

STATIC_URL = '/static/'
STATIC_ROOT = '[full path]/static/'
